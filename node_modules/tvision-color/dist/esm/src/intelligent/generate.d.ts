import type { IgeneratePalette, ColorSpaceArray } from '../type';
/**
* 单个色域范围生成颜色
* @param {number} count 生成个数
* @param {IgeneratePalette} settings 用户配置
* @return {string[]}
*/
export declare function generateColors(count: number, settings: IgeneratePalette): string[];
/**
* 色域范围组生成颜色
* @param {number} count 生成个数
* @param {IgeneratePalette} settings 用户配置
* @param {ColorSpaceArray} colorSpaceGroup 色域范围组
* @return {string[]}
*/
export declare const generatePaletteByGroup: (count: number, settings: IgeneratePalette, colorSpaceGroup: ColorSpaceArray[]) => string[];
/**
* 生成颜色
* @param {number} count 生成个数
* @param {IgeneratePalette} settings 用户配置
* @return {string[]}
*/
export declare function generatePalette(count: number, settings: IgeneratePalette): string[];
