import type { ColorType } from '../../type';
/**
 *
 * @param input 输入检测的数据，可以是对比度值(number)，或一对rgb颜色的数组
 * @param inspectType 检测类型
 * - `normalText` - 普通文本
 * - `largeText` - 大型文字
 * - `nonText` - 图形等非文本内容
 *
 * 详见:
 *
 * https://www.w3.org/TR/WCAG21/#contrast-minimum
 * https://www.w3.org/TR/WCAG21/#contrast-enhanced
 * https://www.w3.org/TR/WCAG21/#non-text-contrast
 *
 * @returns WCAG 2.1 AA&AAA 检测结果
 */
export declare function wcag21(input: number | [ColorType, ColorType], inspectType: 'normalText' | 'largeText' | 'nonText'): {
    contrast: number;
    AA: boolean;
    AAA: boolean | null;
};
