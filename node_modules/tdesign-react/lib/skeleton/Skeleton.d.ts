import React from 'react';
import { TdSkeletonProps } from './type';
import { StyledProps, TNode } from '../common';
export declare type SkeletonProps = TdSkeletonProps & StyledProps & {
    children: TNode;
};
declare const Skeleton: {
    (props: SkeletonProps): React.ReactNode;
    displayName: string;
    defaultProps: TdSkeletonProps;
};
export default Skeleton;
